server:
  port: 8888

spring:
  application:
    name: "cloud-config-server"
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/spring/cloud/config-files
          search-paths: "*/*"

